import { makeStyles, createStyles } from "@mui/styles";

export const useDisImgStyles = makeStyles((theme) =>
  createStyles({
    root: { width: "100%", height: "100%" },
    home: { width: "70px", height: "70px" },
  })
);
